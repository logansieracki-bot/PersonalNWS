# Third-party software

PersonalNWS Stage 1 uses:

- **MapLibre GL JS** — interactive browser map rendering.
- **Vite** — static application build tooling.
- **Playwright** — browser smoke testing in GitHub Actions.

Stage 1 contains no NEXRAD decoder, Rust/WASM radar dependencies, or radar-data libraries.
